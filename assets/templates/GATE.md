# GATE — {{what is gated}}

status: locked
<!-- lifted grammar: status: lifted 2026-01-31 "{{the owner's exact words}}" -->

## Why this gate exists
{{The risk that must be resolved before this work may exist — legal exposure,
irreversible action, spend, a compliance status still TBD.}}

## Preconditions to lift
- [ ] {{e.g. the entity/account/approval actually exists}}
- [ ] {{e.g. the design doc is reviewed}}
- [ ] {{e.g. the owner has said the words}}

## Lift protocol
The owner lifts the gate in-session, naming the preconditions met. Record the
lift on the status line above with date + quote. Once recorded, it is never
re-asked — a recorded decision is a closed question.

## Iron rules (survive the lift)
- {{e.g. no real PII in this folder, placeholders only}}
- {{e.g. the compatibility matrix stays server-side}}

## Pre-scripted refusals
- "Just build it locally, only for me" → "The gate covers existence, not
  visibility — status is locked; ask {{owner}} to lift it."
- "It's just a prototype" → "Prototypes are code; the gate is lifted or it
  isn't."
