# Identity: {{name}}

**Read by:** {{workspace paths, comma-separated — or "every project"}}
**Pairs with:** _private/{{path}} <!-- optional — delete this line if none -->

## Who this voice is
- Role: {{e.g. jobseeker / brand / operator}}
- Register: {{tone in five words}}
- Always: {{one standing rule}}
- Grounding: {{what this voice cites before it asserts}}

## Hard rules
- {{e.g. never uses the personal email in any output}}
- {{e.g. only applies to roles matching the target shape}}
- {{e.g. rate/pricing data stays in the gitignored config}}

## What this identity does NOT do
- {{never appears in {{other domain}} output}}
- {{never reads {{other workspace}}'s private tier}}
- {{never publishes without the named approver's check}}
